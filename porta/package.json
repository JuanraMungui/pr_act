{
  "name": "portafolio-juanra",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && npm run purge",
    "purge": "purgecss --css dist/assets/*.css --content dist/index.html dist/assets/*.js --output dist/assets/ --safelist spin",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.10",
    "less": "^4.2.0",
    "purgecss": "^6.0.0",
    "gh-pages": "^6.1.1"
  }
}
